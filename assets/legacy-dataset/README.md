# Legacy Dataset

This folder contains the XML files required to seed `freevibes-v2` as a self-contained repository.

- Source: original FreeVibes legacy XML dataset
- Purpose: reproducible local and Docker seeding without depending on a sibling folder outside this repository
- Integrity: the original `freevibes-legacy` snapshot remains untouched and separate
